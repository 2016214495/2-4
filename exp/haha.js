const newStudent = require('./student');
exports.newStudent = function(name) {
    name.forEach(function(item, index) {
        var ab = item;
        var students = item.students;
        ad.newStudent(students);
    })
}
//newStudent('Lily');